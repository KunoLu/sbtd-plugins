# Maestro Mobile E2E Lessons Index

Read this index only after a Maestro setup, run, device inspection, or report generation failure.

Load a referenced lesson only when the tags, keywords, platform, version, or error text match the current failure.

| Lesson | Tags | Keywords | Reference |
|---|---|---|---|
| iOS real-device setup, driver patch, transport, app build, and device mode | `maestro`, `ios`, `real-device`, `xcode`, `signing`, `java`, `driver`, `view-hierarchy`, `tap` | `maestro-cli-2.6.1.jar`, `driver/ios/MaestroDriverLib`, `driver-setup`, `pymobiledevice3`, `usbmux`, `iproxy`, `ScreenSizeHelper.swift`, `Fatal error: Not implemented yet`, `.faceUp`, `.faceDown`, `status bar hierarchy`, `TARGETED_DEVICE_FAMILY`, `launchApp`, `clearAppState` | `ios-real-device-setup.md` |

