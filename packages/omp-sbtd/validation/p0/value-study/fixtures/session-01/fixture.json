{
  "schemaVersion": 1,
  "id": "P0-VS-SESSION-01",
  "category": "session-resume-compaction",
  "prompt": "Fix a local Session compaction replay so it restores selected workflow facts but recomputes derived control state from a fresh observation.",
  "startingFiles": {
    "README.md": "# P0 frozen fixture\n\nThis fixture is self-contained, credential-free, and may be copied into an isolated study workspace.\n"
  },
  "startingSnapshotSha256": "4439ce20a98f21b38d42fe2a30d2b2cf7791b0c47475f56671a889ea5c09978d",
  "expected": {
    "route": "legacy-safe-change",
    "routeCost": "standard",
    "requiredGates": ["legacy-change-safety", "refactoring-pass", "tdd"],
    "obligations": [
      "Preserve Session-owned choices",
      "Recompute derived control state"
    ]
  },
  "rubric": [
    {
      "id": "replay",
      "description": "The Session replay restores only persisted choices and recomputes derived state.",
      "weight": 60,
      "severe": true
    },
    {
      "id": "regression-safety",
      "description": "The regression covers compaction and fresh observation semantics.",
      "weight": 40,
      "severe": false
    }
  ],
  "cleanupBoundary": "Remove only artifacts created inside this isolated fixture copy.",
  "permittedNetwork": "omp-owned-model-only"
}
